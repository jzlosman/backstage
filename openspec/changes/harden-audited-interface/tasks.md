## 1. Recoverable keyboard shell

- [x] 1.1 Add regression tests for effective ledger visibility, narrow master-detail return, and saved preference behavior
- [x] 1.2 Implement recoverable ledger visibility and named show/hide affordances
- [x] 1.3 Add regression tests for command-palette focus containment and trigger restoration
- [x] 1.4 Implement modal focus wrapping and background isolation
- [x] 1.5 Add regression tests for keyboard pane resizing and the advertised refresh shortcut
- [x] 1.6 Implement accessible separator ranges, keyboard resizing, frame-bounded pointer resizing, and refresh shortcut handling

## 2. Identifiable responsive work

- [x] 2.1 Add regression tests for per-project planning-file counts and planning-candidate language
- [x] 2.2 Derive indexed member counts and render clear full and compact project identities
- [x] 2.3 Replace vague candidate labels and detector evidence with factual user-facing copy
- [x] 2.4 Adapt the titlebar, filters, file selectors, project rail, and reading desk for 320 px, 680 px, coarse pointers, long text, and 200% zoom
- [x] 2.5 Add a regression test proving projects without indexed planning files are absent from the registry and aggregate counts
- [x] 2.6 Filter project navigation and workspace project counts to indexed planning work

## 3. Efficient coherent presentation

- [x] 3.1 Add a focused test proving unchanged Markdown is not reparsed during shell-only updates
- [x] 3.2 Memoize Markdown rendering at the artifact boundary and verify frame-bounded resizing
- [x] 3.3 Add `@phosphor-icons/react`, replace one-off interface SVGs with explicit shared icon imports, and keep accessible names and tooltips
- [x] 3.4 Consolidate semantic color tokens and strengthen focus and control-boundary contrast
- [x] 3.5 Replace blanket reduced-motion timing with intentional static scan, pulse, and transition states

## 4. Verification

- [x] 4.1 Run frontend tests, typecheck, lint, Rust workspace tests, formatting, Clippy, and the pure-core boundary check
- [x] 4.2 Run the Impeccable detector once over changed UI targets and resolve verified findings
- [x] 4.3 Perform one batched browser pass at desktop, 960 px, 680 px, and 320 px covering pointer, keyboard, focus, counts, long content, and reduced motion
- [x] 4.4 Compare production bundle size, inspect the final diff, and obtain a bounded code review
- [x] 4.5 Build and smoke-test the packaged macOS app, relaunch it, and verify scanned repositories remain unchanged and path containment tests pass
- [x] 4.6 Re-run frontend verification, validate OpenSpec, rebuild the packaged app, and relaunch it
